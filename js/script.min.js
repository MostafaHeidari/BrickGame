import Game from"./game.js";let canvas=document.querySelector("#GameScreen"),ctx=canvas.getContext("2d");const GameWidth=800,GameHeight=600;let game=new Game(800,600),lastTime=0;function GameLoop(e){let a=e-lastTime;lastTime=e,ctx.clearRect(0,0,800,600),game.update(a),game.draw(ctx),requestAnimationFrame(GameLoop)}requestAnimationFrame(GameLoop);